# 阻塞与自动绕行记录

本文件只记录会影响继续推进、可能诱导错误结论，或需要保护冻结 reference 的问题。
出现问题后优先自动重试、切换低开销探针或转向静态/独立实现，不等待人工逐项确认。

## 已绕过：Unidbg/Unicorn 在 macOS ARM64 的偶发 JVM native crash

- 现象：独立 `SignerOneClick` 诊断进程偶发 `Abort trap: 6`，JVM 报告 crash 位于
  native code，并生成 `hs_err_pid*.log`。
- 范围：只在反复启动诊断/oracle 进程时偶发；不是 signer 算法不一致，也不是冻结
  Pixel reference 失败。
- 已有工程防护：普通 `run-java.sh --mode=both` 已把 native/V4/V5 emulator 生命周期
  隔离到不同 JVM；测试进程也禁止复用 emulator JVM。
- 自动处理：删除本次诊断产生的 `hs_err_pid*.log`，使用全新 JVM 原命令重试；不要改
  `device-reference/references/pixel8-api36/reference-result.json` 来掩盖问题。
- 当前状态：两次诊断首跑出现过，重试后原 SO oracle 正常完成；工作未因此停止。
- 后续替代路线：优先使用已恢复的独立 C++ 做快速多配置计算，仅在需要新增 oracle
  证据时启动一次原 SO/Unidbg 进程。

## 保护规则：冻结 Pixel reference 不可改写

- 文件：`/Users/sanbo/Desktop/api/qbdi/device-reference/references/pixel8-api36/reference-result.json`
- 当前 SHA-256：`b163eb800b2a425158f6b825e8e9439b4b9bd1bca8b0ed0be2a155f2ef9ceca0`
- 处理原则：若新实现不匹配，追输入、环境 correction、payload、IV、ciphertext、tag
  的第一个分歧；禁止更新 reference 让测试“变绿”。

